{
  "name": "stratum-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "db:reset": "supabase db reset && echo '\n⚠️  Please refresh your browser! ⚠️\n'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.56.0",
    "react": "19.3.0"
  },
  "devDependencies": {
    "playwright": "^1.56.1",
    "vercel": "^50.4.8"
  }
}
